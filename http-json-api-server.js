const http=require("http");const u=require("url");http.createServer((req,res)=>{const p=new URL(req.url,"http://x");const d=new Date(p.searchParams.get("iso"));let o;if(p.pathname==="/api/parsetime")o={hour:d.getHours(),minute:d.getMinutes(),second:d.getSeconds()};else if(p.pathname==="/api/unixtime")o={unixtime:d.getTime()};if(o){res.writeHead(200,{"Content-Type":"application/json"});res.end(JSON.stringify(o))}else res.end()}).listen(process.argv[2])